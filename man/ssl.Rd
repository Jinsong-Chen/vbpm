% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pefa_methods.R
\name{ssl}
\alias{ssl}
\title{Sums of squared loadings for a PEFA sweep}
\usage{
ssl(x)
}
\arguments{
\item{x}{A result returned by \code{\link[=pefa]{pefa()}}.}
}
\value{
A list with one element per candidate, named as \code{x$loadings} is
(the candidate \code{K} as a character string). Each element is the double
vector \code{colSums(L^2)} of that candidate's loading matrix and keeps the
stored loading-column names when the matrix has them. In bifactor mode
each vector covers all \code{K + 1} stored columns with the general factor
first, so the group-block scale used by every between-candidate
comparison is \code{ssl(x)[[k]][-1]}.
}
\description{
Descriptive column sizes for every candidate in a \code{\link[=pefa]{pefa()}} sweep:
\code{colSums(Lam^2)} on the stored loading matrix of each candidate. They are
not Gram eigenvalues, they take part in no correspondence or selection
rule, and no cutoff is applied to them. \code{summary()} calls this function, so
there is one definition.
}
\details{
A low \code{phi_min} in \verb{$transitions} or \verb{$persistence$phi} is a weakest-link
reading over every source column, including one the fit has driven to
nearly zero. Such a column matches the smallest-norm target and carries an
essentially arbitrary congruence. \code{ssl()} is what separates "this candidate
contains a column with nothing in it" from "the retained structure moved".
}
\seealso{
\code{\link[=pefa]{pefa()}}
}
