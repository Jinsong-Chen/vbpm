% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pefa_accessors.R
\name{ssl}
\alias{ssl}
\title{Sums of squared loadings for a PEFA sweep}
\usage{
ssl(x)
}
\arguments{
\item{x}{A result returned by \code{\link[=pefa]{pefa()}}.}
}
\value{
A list with one element per candidate, named as \code{x$loadings} is
(the candidate \code{K} as a character string). Element vectors are unnamed.
In bifactor mode each vector covers all \code{K + 1} stored columns with the
general factor first, so the group-block comparison scale is
\code{ssl(x)[[k]][-1]}.
}
\description{
Descriptive column sizes for every candidate in a \code{\link[=pefa]{pefa()}} sweep:
\code{colSums(Lam^2)} on the stored loading matrix of each candidate. They are
not Gram eigenvalues and take part in no selection rule. \code{summary()} calls
this function, so there is one definition.
}
\seealso{
\code{\link[=pefa]{pefa()}}, \code{\link[=persistence]{persistence()}}
}
