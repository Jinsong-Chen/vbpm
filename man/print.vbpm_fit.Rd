% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vbpm_fit.R
\name{print.vbpm_fit}
\alias{print.vbpm_fit}
\title{Compact display of a fitted vbpm model}
\usage{
\method{print}{vbpm_fit}(x, tau = 0.5, ...)
}
\arguments{
\item{x}{A fit returned by \code{\link[=vbfa]{vbfa()}} or \code{\link[=vbmimic]{vbmimic()}}.}

\item{tau}{Threshold on the posterior inclusion probability used only for
the "active loadings" line (default \code{0.5}, matching \code{\link[=vb_fit]{vb_fit()}}).}

\item{...}{Ignored (present for compatibility with the \code{\link[=print]{print()}} generic).}
}
\value{
\code{x}, invisibly (the R convention for print methods, so \code{print(fit)}
can be used in pipes without losing the object).
}
\description{
Prints a short summary — model type, dimensions, convergence, timing, and
selection results — instead of dumping the full list. Access the parts as
usual (\code{fit$Lam} etc.; see \link{vbpm_fit}); \code{str(fit)} shows everything.
}
\examples{
sim <- sim_fa(N = 300, K = 3, ipf = 6, lam = .7, lac = .3, rseed = 1)
Q <- matrix(-1L, ncol(sim$dat), 3)
for (k in 1:3) { a <- which(rep(1:3, each = 6) == k)[1:2]
                 Q[a, ] <- 0L; Q[a, k] <- 1L }
fit <- vbfa(sim$dat, Q)
fit               # dispatches here: compact summary, not a list dump
fit$Lam[1:3, ]    # the object is still an ordinary list underneath

}
