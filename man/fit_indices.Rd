% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_indices.R
\name{fit_indices}
\alias{fit_indices}
\title{SEM-like fit statistics for a vbfa model}
\usage{
fit_indices(
  fit,
  Y,
  Q,
  tau = 0.5,
  gamma = 0.5,
  orthogonal = FALSE,
  rank_adjust = TRUE,
  rank_max_J = 100
)
}
\arguments{
\item{fit}{A fitted object returned by \code{\link[=vbfa]{vbfa()}} (diagonal-residual model;
local-dependence fits are not yet supported and raise an error).}

\item{Y}{The \verb{N x J} data matrix the model was fit to.}

\item{Q}{The \verb{J x K} loading design matrix used in the fit.}

\item{tau}{Hard-selection threshold on the posterior inclusion probability
(default \code{0.5}).}

\item{gamma}{Reserved (extended BIC tuning); currently unused in the
returned vector.}

\item{orthogonal}{Logical; must match the value used in \code{\link[=vbfa]{vbfa()}}. When
\code{TRUE}, factor correlations are not free parameters.}

\item{rank_adjust}{Logical; if \code{TRUE}, the effective parameter count is the
rank of the Jacobian of the model-implied covariance (needs the \code{numDeriv}
package). Automatically skipped for large \code{J}.}

\item{rank_max_J}{Skip the Jacobian rank check when \code{J} exceeds this value
(default \code{100}); the nominal parameter count is used instead.}
}
\value{
A named numeric vector: \code{t_nom}, \code{t}, \code{t_S} (parameter counts),
\code{RMSEA}, \code{SRMR}, \code{CFI}, \code{TLI}, \code{AIC}, \code{BIC} (hard selection), \code{AIC_S},
\code{BIC_S} (soft selection), and \code{ELBO}.
}
\description{
Computes structural-equation-model fit statistics for a fitted \code{\link[=vbfa]{vbfa()}}
object under hard selection (an unspecified loading counts as active when its
posterior inclusion probability is at least \code{tau}). Absolute indices
(RMSEA, SRMR, CFI, TLI) and information criteria (AIC, BIC) are returned,
together with the model's ELBO.
}
\references{
Chen, J., & Jin, Y. (2026). Recovering latent structures after variational
Bayesian variable selection: Fit assessment and factor-number selection in
partially exploratory factor analysis. \emph{arXiv preprint} arXiv:2607.07159.

Jin, Y., & Chen, J. (2025). Regularized variational approximation for
partially confirmatory factor analysis. \emph{Structural Equation Modeling: A
Multidisciplinary Journal}, 32(3), 437–449.
\doi{10.1080/10705511.2024.2432612}
}
\seealso{
\code{\link[=vbfa]{vbfa()}}, \code{\link[=pefa]{pefa()}}
}
