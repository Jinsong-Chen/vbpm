% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pefa.R
\name{transition_detail}
\alias{transition_detail}
\alias{transition_detail.pefa}
\title{Per-column detail behind one transition}
\usage{
transition_detail(object, K_from, ...)

\method{transition_detail}{pefa}(object, K_from, ...)
}
\arguments{
\item{object}{A \code{\link[=pefa]{pefa()}} result.}

\item{K_from}{The lower factor number of the transition (\code{K_from -> K_from + 1}).}

\item{...}{Unused.}
}
\value{
A list with \code{K_from}, \code{K_to}, \code{phi} (per matched column),
\code{matched} (larger-solution column indices), \code{sign}, \code{surplus} (index) and
\code{surplus_loadings}, plus the tolerances used; or \code{NULL} when detail is
unavailable.
}
\description{
Returns the ragged material summarized by one row of a sweep's
\verb{$transitions} table: the per-column congruences, which larger-solution
column each smaller-solution column was matched to, the sign multipliers,
and the unmatched (surplus) column with its full loading vector.
}
\details{
The accessor never bypasses the gates of \code{\link[=pefa]{pefa()}}: a transition inside an
orthogonal/bifactor sweep, or one with a non-converged endpoint, returns
\code{NULL} with the reason, because those loadings are not interpretable even
when the fits are retained. It also returns \code{NULL} when \code{keep_fits}
discarded either endpoint.
}
\seealso{
\code{\link[=pefa]{pefa()}}
}
