% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pefa_verify.R
\name{verify_pefa}
\alias{verify_pefa}
\title{Verify the integrity of a PEFA evidence object}
\usage{
verify_pefa(x, Y = NULL)
}
\arguments{
\item{x}{An object of class \code{"pefa"} returned by \code{\link[=pefa]{pefa()}}.}

\item{Y}{Optional. The original \code{N} by \code{J} data matrix the object was
fitted to. When supplied, \code{standardized_data_sha256} is recomputed from
it through the same internal standardization \code{\link[=pefa]{pefa()}} applies before
fitting. When \code{NULL} (the default) the stored data fingerprint is checked
only as part of the evidence payload; see Details for exactly what that
does and does not prove.}
}
\value{
\code{invisible(TRUE)} when every applicable check passes. There is no
other return value: the rebuilt tables and recomputed fingerprints stay
inside the call, since 0.9.0 exports nothing that returns rederived
evidence. Any failure is a controlled error naming the component, never a
\code{FALSE}. \code{x} is never modified.
}
\description{
The sole public integrity checker for objects returned by \code{\link[=pefa]{pefa()}}. It
accepts the current evidence schema and engine semantics only, rebuilds
\verb{$transitions} and \verb{$persistence} from the stored primitive components with
the installed package, and recomputes the Q0, settings, evidence, and
lineage fingerprints. Supplying the original data additionally verifies the
standardized-data fingerprint. It returns \code{invisible(TRUE)} when every
applicable check passes and otherwise raises an error naming the component
that failed.
}
\details{
\code{verify_pefa()} answers one question: is this object still exactly the
evidence the installed package sealed? It runs the checks below and reports
every one that fails, so a single call locates the damage rather than
stopping at the first symptom.

\strong{Compatibility.} \verb{$provenance$evidence_schema_id} and
\verb{$provenance$engine_semantics_id} must equal the identifiers frozen in this
installation. An object written under any other schema or engine is refused
outright rather than checked, because neither its payload nor its hash
canonicalization is this package's. \code{package_version} and
\code{package_build_id} are not compatibility gates and are not required to
match the running installation.

\strong{Rebuilt tables.} \verb{$transitions} is rebuilt from \verb{$sweep}, and
\verb{$persistence} from \verb{$sweep}, \verb{$loadings}, \verb{$pips}, \verb{$Q0}, and the
\code{stability_eps} and \code{bifactor} settings, using the installed table builders
and matcher. \code{\link[=vbfa]{vbfa()}} is never called and nothing is refitted. The rebuilt
tables are used for comparison only and are never returned.

\strong{Fingerprints.} \code{Q0_sha256} is recomputed from \verb{$Q0}, \code{settings_sha256}
from \verb{$settings}, \code{evidence_sha256} from the stored evidence payload, and
\code{lineage_sha256} from the stored evidence hash together with the
parent/reused/newly-fitted declaration. Elapsed \verb{$sweep$secs} lies outside
the evidence payload by design, so the same analysis timed on a slower
machine is not a mutation. Because \code{lineage_sha256} is a function of
\code{evidence_sha256}, it is checked only once that hash agrees.

\strong{Comparison policy.} Stored and rebuilt tables are compared under the
policy the package fixes for its evidence tables, stated here so that
downstream reporting can rely on it: \code{identical()} for integers, logicals,
status/reason character vectors, \code{r}, dimnames, storage modes, and the NA
pattern; \code{expect_equal(tolerance = 1e-10)} for double metric cells. The
implementation applies base R's \code{all.equal(tolerance = 1e-10)} to the
double columns, which is the comparison \code{testthat::expect_equal()}
performs, and \code{identical()} to column names and order, row names,
per-column storage modes, every non-double column, and the NA and NaN
patterns of the double ones. Fingerprints are never compared at a
tolerance: they are compared with \code{identical()}.

\strong{What \code{Y = NULL} does and does not prove.}
\code{standardized_data_sha256} is one of the fields covered by
\code{evidence_sha256}. With \code{Y = NULL} the stored data fingerprint is therefore
verified to be the value that was sealed with the rest of the payload:
nobody has swapped the recorded data identity while leaving the evidence
intact. That is all it proves. No preimage is reconstructed, no data set is
examined, and no claim is made about which data the object was fitted to.
Supplying \code{Y} adds exactly that claim and only that claim: the supplied
matrix, standardized the way \code{\link[=pefa]{pefa()}} standardizes its input, hashes to the
stored value, so it is the fitted input in the same row and column order
and with the same missingness pattern.

The function makes no scientific decision. It does not judge convergence,
persistence, or factor count, and a passing result says nothing about
whether the evidence supports any conclusion.
}
\examples{
## The shipped R8 integration fixture carries a complete, classed evidence
## object together with the exact data that produced its standardized-data
## fingerprint, so both forms of the check run without fitting anything.
path <- system.file("extdata", "vbpm_r8_fixture.rds", package = "vbpm")
if (nzchar(path) && file.exists(path)) {
  integrity <- readRDS(path)[["integrity"]]
  if (!is.null(integrity)) {
    verify_pefa(integrity[["object"]])                    # TRUE, invisibly
    verify_pefa(integrity[["object"]], integrity[["Y"]])  # also checks Y
  }
}

}
\seealso{
\code{\link[=pefa]{pefa()}}, \code{\link[=persistence]{persistence()}}, \code{\link[=ssl]{ssl()}}
}
