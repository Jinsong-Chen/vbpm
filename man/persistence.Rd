% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pefa_accessors.R
\name{persistence}
\alias{persistence}
\alias{print.vbpm_persistence}
\title{Persistence triangle for a PEFA sweep}
\usage{
persistence(
  x,
  metric = c("phi_min", "rmsd", "rmsd_max", "ari", "pip_rmsd", "unmatched_ssl",
    "n_collisions")
)

\method{print}{vbpm_persistence}(x, digits = 2, ...)
}
\arguments{
\item{x}{A result returned by \code{\link[=pefa]{pefa()}}; for the print method, the matrix
\code{persistence()} returned.}

\item{metric}{Name of the stored pair metric to pivot. One of \code{"phi_min"},
\code{"rmsd"}, \code{"rmsd_max"}, \code{"ari"}, \code{"pip_rmsd"}, \code{"unmatched_ssl"}, or
\code{"n_collisions"}. The enumeration is closed; see Details.}

\item{digits}{Number of decimals used when printing the triangle.}

\item{...}{Further arguments. Unused.}
}
\value{
\code{persistence()} returns a \verb{W x W} matrix of class
\code{c("vbpm_persistence", "matrix", "array")}, integer for \code{"n_collisions"}
and double otherwise. It carries three logical matrices of the same \code{dim}
and \code{dimnames} as attributes: \code{"collision"} (the stored collision flag),
\code{"reduced"} (\code{n_pairs < K_from}), and \code{"fit_ok"}
(\code{from_fit_ok & to_fit_ok}). Each is \code{NA} only where its own underlying
factual field is unavailable. \code{print()} returns its input invisibly.
}
\description{
Pivots the long-format \verb{$persistence} table of a \code{\link[=pefa]{pefa()}} object into an
upper-triangular \verb{W x W} matrix holding one stored metric: rows are the
source count \code{k}, columns the target count \code{l}, and both dimnames are
\code{as.character(x$sweep$K)}. The diagonal and the lower half are \code{NA}.
Nothing is recomputed, so switching \code{metric} is free and cannot disagree
with the table.
}
\details{
A cell is \code{NA} when the selected metric itself is unavailable, whether
because the matcher input was unavailable or because that metric's own
narrower dependencies were unmet. Collided, nonconverged-endpoint, and
reduced-pair-set cells retain any computed value.

The \code{metric} enumeration is \strong{closed} and deliberately excludes
\code{unmatched_n}, \code{source_ineligible_n}, \code{target_eligible_n}, and \code{n_pairs}:
those are matching-process counts rather than structural measurements, and
a \verb{W x W} triangle is the wrong shape for reading them. They stay in
\verb{$persistence}, which is already long-format and is the natural place to
filter or join on them. \code{backbone_ssl_min} is likewise absent because it is
a per-candidate quantity on \verb{$sweep}, not a pair metric -- a triangle of it
would just repeat one vector twice. Asking for any of these five is an
error that names the table the field lives in.

Cells are \strong{not on a common pair-set scale.} \code{phi_min} in row \code{k} is a
minimum over at most \code{k} pairs, so rows are not directly comparable, and
within a row a \code{~} cell scored fewer columns than its neighbours. The
\code{k = K0} row is backbone-only throughout, and by construction carries
\strong{no} \code{~}: its pair set is complete for that source
(\verb{n_pairs == K_from == K0}), it is simply a small source. A high value
there says the anchors did not move, not that an exploratory structure
persisted. \code{n_pairs} gives the exact count.

The printed \verb{?} marks \strong{selected-metric unavailability only}. It is not
the complete tri-state edge state, which a decision profile computes from
\code{pair_status}, the chosen metric's own \code{NA}, and its declared endpoint,
collision, and eligibility conditions.

The three markers get equal display weight on purpose: each is a fact that
changes how the printed number should be read, and singling one out for a
glyph would reintroduce a package preference through the print method.
}
\seealso{
\code{\link[=pefa]{pefa()}}, \code{\link[=ssl]{ssl()}}
}
